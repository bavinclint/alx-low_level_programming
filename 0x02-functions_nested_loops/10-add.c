#include "main.h"
/**
 * add - adds two integers and returns the result
 * @a: first parameter
 * @b: second parameter
 * Return: returns the result
 */
int add(int a, int b)
{
	int c;

	c = a + b;

	return (c);
}
